language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get install -qq freeglut3-dev
before_script:
  - autoreconf -i
script: cmake . && make && ./ndkmol --version
branches:
  only:
    - master
notifications:
  recipients:
    - wojdyr@gmail.com
  email:
    on_success: change
    on_failure: always
